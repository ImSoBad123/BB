if game.Workspace.Map:FindFirstChild("Arena") then
    for _, child in ipairs(game.Workspace.Map.Arena:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("Quarry") then
    for _, child in ipairs(game.Workspace.Map.Quarry:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("RingOfFire") then
    for _, child in ipairs(game.Workspace.Map.RingOfFire:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("Desert") then
    for _, child in ipairs(game.Workspace.Map.Desert:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("Classic_V2") then
    for _, child in ipairs(game.Workspace.Map.Classic_V2:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("Heaven") then
    for _, child in ipairs(game.Workspace.Map.Heaven:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("Height_Fight") then
    for _, child in ipairs(game.Workspace.Map.Height_Fight:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("WinterCountryside") then
    for _, child in ipairs(game.Workspace.Map.WinterCountryside:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("Enchanted_Desert") then
    for _, child in ipairs(game.Workspace.Map.Enchanted_Desert:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("PolarExpress") then
    for _, child in ipairs(game.Workspace.Map.PolarExpress:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("Grassy_Classic") then
    for _, child in ipairs(game.Workspace.Map.Grassy_Classic:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("Ocean") then
    for _, child in ipairs(game.Workspace.Map.Ocean:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("Underworld") then
    for _, child in ipairs(game.Workspace.Map.Underworld:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("Jungle") then
    for _, child in ipairs(game.Workspace.Map.Jungle:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
elseif game.Workspace.Map:FindFirstChild("Classic") then
    for _, child in ipairs(game.Workspace.Map.Classic:GetChildren()) do
        if child.Name == "Sock" then
            child.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
else
print("ko thay map")
end
